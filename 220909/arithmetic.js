// Arithmetic operations

const x = 40
const y = 2
const z = 5
console.log("(x + y) * z:", (x + y) * z);

let ii = 0
console.log("ii++ + 3:", ii++ + 3, ii);


// * = multiply
// / = divide
// % = modulus (remainder)

// OPERATOR PRECEDENCE
console.log("a:", a);
